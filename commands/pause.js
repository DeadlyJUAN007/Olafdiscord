// const discord = require("discord.js");

module.exports.run = async (client, message, args, options) => {

//     var guildIDData = options.active.get(message.guild.id);

//     if (!guildIDData) return message.channel.send("❌**Nothing playing in this server.**");

//     if (message.member.voice.channel != message.guild.me.voice.channel) return message.channel.send("You aren't in the same voice channel.");

//     if (guildIDData.dispatcher.paused) return message.channel.send("Already paused.");

//     guildIDData.dispatcher.pause();

//     return message.channel.send("**Paused** ⏸");

}

module.exports.help = {
//     name: "pause",
//     description: "Pause the current song playing.",
//     category: "Music",
//     aliases: ["pauzeer", "pauze"]
}

// // Usage: **!pause**